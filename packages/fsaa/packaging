#!/usr/bin/env bash

set -eux

source meta-info/blobs-versions.env

mkdir -p "${BOSH_INSTALL_TARGET}/bosh-utils"
cp -a "${BOSH_COMPILE_TARGET}/bosh-utils/"* "${BOSH_INSTALL_TARGET}/bosh-utils"

mkdir -p "${BOSH_INSTALL_TARGET}/bin"
tar zxf "fsaa/fsaa_${FSAA_VERSION}.tar.gz" --strip 1 -C "${BOSH_INSTALL_TARGET}/bin"
